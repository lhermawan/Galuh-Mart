name: galuh_mart
description: A Flutter marketplace prototype for UMKM Perum Kota Galuh.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  dio: ^5.7.0
  cached_network_image: ^3.4.1
  shared_preferences: ^2.3.2
  flutter_secure_storage: ^9.2.2
  firebase_messaging: ^15.1.3
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
